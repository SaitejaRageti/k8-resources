apiVersion: v1
kind: Pod
metadata:
  name: config-pod-env
  labels:
    project: roboshop
    tier: frontend
    environment: dev
spec:
  containers:
    - name: nginx
      image: nginx
      envFrom:
        - ConfigMapRef:
            name: config